[package]
name = "tai-proto"
version = "0.1.0"
edition = "2024"

[dependencies]
serde.workspace = true
bincode = { version = "2", features = ["serde"] }
tokio = { workspace = true, features = ["io-util"] }
